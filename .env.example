# Example environment variables for jansan-final-project-uki
# Backend
MONGO_URI=mongodb://localhost:27017/jansan
JWT_SECRET=replace_this_with_a_strong_secret
PORT=3003
STRIPE_SECRET_KEY=sk_test_your_test_key_here

# If you are using MongoDB Atlas, make sure your current machine IP is allowed in
# Network Access (Atlas UI) or set the IP to 0.0.0.0/0 for development (less secure).
# See: https://www.mongodb.com/docs/atlas/security-whitelist/

# Frontend (Vite)
# Set this to override the API URL used by the frontend during development
VITE_API_URL=http://localhost:3003/api

# Vite env var for Google OAuth
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
